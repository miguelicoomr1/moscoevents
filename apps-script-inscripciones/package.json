{
  "name": "mosco-events-apps-script-inscripciones",
  "private": true,
  "version": "1.0.0",
  "description": "Herramientas para desplegar google-apps-script-inscripciones.js (backend de inscripciones, cuenta inscripciones@moscoevents.com) con clasp.",
  "scripts": {
    "sync": "node sync.js",
    "push": "npm run sync && clasp -u inscripciones push",
    "deploy": "npm run push && node deploy.js"
  }
}
