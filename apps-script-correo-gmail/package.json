{
  "name": "mosco-events-apps-script-correo-gmail",
  "private": true,
  "version": "1.0.0",
  "description": "Mensajero de respaldo: mismo google-apps-script-correo.js desplegado con moscoeventes@gmail.com, para cuando el dominio no pueda entregar correo externo.",
  "scripts": {
    "sync": "node sync.js",
    "push": "npm run sync && clasp push",
    "deploy": "npm run push && node deploy.js"
  }
}
